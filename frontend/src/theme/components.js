const components = {
    MuiButton: {
        styleOverrides: {
            root: { borderRadius: 8, padding: '10px 22px' },
        },
    },
    MuiCard: {
        styleOverrides: {
            root: { borderRadius: 16, boxShadow: '0 2px 8px rgba(0,0,0,0.05)' },
        },
    },
};

export default components;
