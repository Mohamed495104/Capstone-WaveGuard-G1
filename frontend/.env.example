# ========================================
# FRONTEND ENVIRONMENT VARIABLES
# ========================================
# Copy this file to .env.local and fill in your actual values
# NEVER commit .env.local to git!
# 
# Note: All frontend environment variables MUST start with NEXT_PUBLIC_
# to be accessible in the browser

# API Configuration
# Backend API URL
# Development: http://localhost:5000
# Production: https://your-backend.railway.app or https://your-backend.onrender.com
# IMPORTANT: NO trailing slash!
NEXT_PUBLIC_API_URL=http://localhost:5000

# Firebase Configuration (Frontend SDK)
# How to get these values:
# 1. Go to Firebase Console (https://console.firebase.google.com)
# 2. Select your project
# 3. Project Settings -> General
# 4. Scroll down to "Your apps" section
# 5. Select Web App or create one
# 6. Copy the config values from SDK setup
# 
# All these values are safe to commit (they're public by design)
# Security is handled by Firebase Security Rules
NEXT_PUBLIC_FIREBASE_API_KEY=your_firebase_api_key_here
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your_project_id.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your_project_id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your_project_id.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
NEXT_PUBLIC_FIREBASE_APP_ID=your_app_id

# Google Places API (Optional - for location autocomplete in profile)
# Get from Google Cloud Console -> APIs & Services -> Credentials
NEXT_PUBLIC_GOOGLE_PLACES_API_KEY=your_google_places_api_key_here

# PayPal Configuration (Required for donation functionality)
# Get from PayPal Developer Dashboard -> My Apps & Credentials
# Use sandbox client ID for testing, live client ID for production
NEXT_PUBLIC_PAYPAL_CLIENT_ID=your_paypal_client_id_here

# ========================================
# PRODUCTION DEPLOYMENT NOTES
# ========================================
# When deploying to Vercel:
# 1. All environment variables must be added in Vercel dashboard
# 2. Settings -> Environment Variables -> Add each variable
# 3. Set NEXT_PUBLIC_API_URL to your backend URL (Railway/Render)
# 4. Vercel will automatically rebuild when you add/change variables
# 5. Environment variables are encrypted and secure in Vercel
# 
# Auto-Deploy:
# - Vercel automatically deploys when you push to main branch
# - Preview deployments created for pull requests
# - No manual action needed after initial setup
# 
# See HOSTING_INSTRUCTIONS.md for complete deployment guide
